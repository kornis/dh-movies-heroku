<%- include('partials/header') %>

    <div class="container mt-5">
        <h1 class="text-center mb-5">Series</h1>
        <div class="row">
            <div class="col">
                <div class="card-container d-flex">
                    <% series.forEach(serie=> { %>
                        <div class="card text-center">
                            <div class="card-body">
                                <h5 class="card-title">
                                    <%= serie.title %>
                                </h5>
                                <p class="card-text">Género: <%= serie.Genre.name %>
                                </p>
                            </div>
                            <div class="card-footer text-muted">
                                <%= serie.release_date.getFullYear() %>
                            </div>
                        </div>
                        <% }) %>

                </div>
            </div>
        </div>
    </div>

</body>

</html>